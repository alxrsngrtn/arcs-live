commit a575e599dda936c59cf711c62a0dee047a4d0639
Author: Scott J Miles <sjmiles@google.com>
Date:   Wed Mar 21 19:48:26 2018 -0700

    ensure AppShell is the Source Of Truth for description, user, share, and metadata;
    use an explicit cache for consumedPlan;
    surface `clone` shallow-copy method from Xen;
    clone metadata when modifying it (immutability contract);
    prevent prop-check for persistent-handles, so it always updates;
