import { Handle } from './handle.js';
export declare class SlotInfo {
    formFactor: string;
    handle: Handle;
    constructor({ formFactor, handle }: {
        formFactor: string;
        handle: Handle;
    });
}
